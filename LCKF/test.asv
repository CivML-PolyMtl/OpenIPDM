dt = 1.5;
A = [1, dt, (dt^2)/2; 0, 1, dt; 0, 0, 1];
constrain_vector = [0, 1, 0];

c = [1, 0, 0];
c_out = binary_matrix(c);

c_bin = binary_matrix(c);
constrain_vector_bin = binary_matrix(constrain_vector);

temp = c_bin * A * constrain_vector_bin';

[row_count, ~] = size(a);
zeros_to_add = row_count - numel(temp);
temp = [temp; zeros(zeros_to_add, 1)];

disp(temp)

c = [0, 1, 0];
indices = find(c == 1);
disp(indices);



